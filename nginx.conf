#PRODUCTION



# # Rate limiting zones
# limit_req_zone $binary_remote_addr zone=api_limit:10m rate=10r/s;
# limit_conn_zone $binary_remote_addr zone=addr:10m;

# # Upstream backend
# upstream isp_backend {
#     server localhost:5000;
#     keepalive 32;
# }

# server {
#     listen 80;
#     listen [::]:80;
    
#     # ⚠️ REPLACE THIS WITH YOUR SERVER IP
#     server_name 64.23.166.55 ;

#     # Security headers
#     add_header X-Frame-Options "SAMEORIGIN" always;
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header X-XSS-Protection "1; mode=block" always;
#     add_header Referrer-Policy "no-referrer-when-downgrade" always;

#     # Logging
#     access_log /var/log/nginx/isp-access.log;
#     error_log /var/log/nginx/isp-error.log warn;

#     # Max upload size
#     client_max_body_size 10M;

#     # Proxy all requests to Node.js
#     location / {
#         # Rate limiting
#         limit_req zone=api_limit burst=20 nodelay;
#         limit_conn addr 10;

#         # Proxy to Node.js
#         proxy_pass http://isp_backend;
#         proxy_http_version 1.1;
        
#         # Headers
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
        
#         proxy_cache_bypass $http_upgrade;
        
#         # Timeouts
#         proxy_connect_timeout 60s;
#         proxy_send_timeout 60s;
#         proxy_read_timeout 60s;
#     }

#     # Health check endpoint (no rate limit)
#     location /health {
#         proxy_pass http://isp_backend;
#         proxy_set_header Host $host;
#         access_log off;
#     }

#     # Block hidden files
#     location ~ /\. {
#         deny all;
#         access_log off;
#         log_not_found off;
#     }
# }






#LOCAL TESTING

# Local Nginx config for development using port 80
worker_processes 1;

events { worker_connections 1024; }

http {

    # Rate limiting zones
    limit_req_zone $binary_remote_addr zone=api_limit:10m rate=10r/s;
    limit_conn_zone $binary_remote_addr zone=addr:10m;

    # Upstream backend
    upstream isp_backend {
        server app:5000;  # use container name, not localhost
        keepalive 32;
    }

    server {
        listen 80;

        # Optional: local server name
        server_name localhost;

        # Proxy all requests to Node.js
        location / {
            limit_req zone=api_limit burst=20 nodelay;
            limit_conn addr 10;

            proxy_pass http://isp_backend;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;

            proxy_connect_timeout 60s;
            proxy_send_timeout 60s;
            proxy_read_timeout 60s;
        }

        # Health check endpoint
        location /health {
            proxy_pass http://isp_backend;
            proxy_set_header Host $host;
            access_log off;
        }

        # Block hidden files
        location ~ /\. {
            deny all;
            access_log off;
            log_not_found off;
        }
    }
}